export const selectAllLangs = (state) => state.language;
// export const selectLangOptions = (state) => {
//   state.language.map((l) => {
//     return { value: `${l.lang}`, label: `${l.lang}` };
//   });
// };
